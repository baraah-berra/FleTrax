import React from 'react'
import Map from '../../components/Map'
type Props = {}

const page = (props: Props) => {
    return (
        <div>
            <Map />
        </div>
    )
}

export default page